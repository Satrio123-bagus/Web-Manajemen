services:
  backend:
    build: ./server
    restart: unless-stopped
    env_file:
      - ./server/.env
    environment:
      - PORT=8080
    networks:
      - insert3coins-net

  frontend:
    build: ./client
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - insert3coins-net

networks:
  insert3coins-net:
    driver: bridge
